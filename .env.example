# Database - PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/finance_mcp

# Logging: DEBUG (human-readable), INFO/WARNING/ERROR (JSON structured)
LOG_LEVEL=INFO

# JWT - change in production
JWT_SECRET=change-me-in-production
JWT_EXPIRE_HOURS=24
